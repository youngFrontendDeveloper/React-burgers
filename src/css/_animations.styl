/*
  Анимация:
  enter
  exit
*/
// Появление заказанного бургера слева
.order-enter
  transition: 0.5s;
  transform: translateX(-120%);
  max-height: 0;

  &.order-enter-active
    transform: translateX(0);
    max-height: 60px;
    padding: 2rem 0 !important;

// Исчезновение удаленного бургера вправо
.order-exit
  transition: 0.5s;
  transform: translateX(0);

  &.order-exit-active
    transform: translateX(120%);
    padding: 0;

// Появление и исчезновение цифр (количества заказанных бургеров) в count
.count-enter
  transition: 0.5s;
  transform: translateY(100%);

  &.count-enter-active
    transform: translateY(0);

.count-exit
  transition: 0.5s;
  transform: translateY(0);
  position: absolute;
  left: 0;
  bottom: 0;

  &.count-exit-active
    transform: translateY(-100%);


// Появление суммы  (стоимости заказанных бургеров) в count*price
.price-enter
  transition: 0.5s;
  transform: translateY(100%);

  &.price-enter-active
    transform: translateY(0);

// исчезновение суммы  (стоимости заказанных бургеров) в count*price
.price-exit
  transition: 0.5s;
  transform: translateY(0);
  position: absolute;
  left: 0;
  bottom: 0;

  &.price-exit-active
    transform: translateY(-100%);